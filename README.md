# TFTIQ
Exposing hidden player builds.

agpl3 

How to use 

In your terminal run: </br>
python -m streamlit run app.py </br>
python -m streamlit run app.py.py </br>

This starts a local dashboard server and automatically opens it in your default web browser (http://localhost:8501).
